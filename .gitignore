# Entornos virtuales
env/
venv/
.venv/
ENV/
.env

# Python
*pyc*
__pycache__/
.pytest_cache/

# Datos y notebooks
raw_data/
.ipynb_checkpoints

# Editor / SO
.vscode/
.idea/
.DS_Store

# Archivos de bloqueo temporales de Microsoft Office (aparecen mientras el
# documento está abierto; nunca deben versionarse)
~$*

# R
.Rhistory
.Rdata
.Rproj.user/

# Directorios temporales de escritura atómica del análisis de ROIs
# (huérfanos si una corrida se interrumpe a mitad de la promoción final;
# nunca deberían contener una salida final, ver README del módulo)
analysis/roi_comparison/outputs/.staging-*/

# Node.js (usado solo para generar el Supplement LOSO vía el paquete `docx`;
# reinstalable con `npm install` desde package.json/package-lock.json, nunca
# se versiona el árbol instalado)
node_modules/

# Copia de resguardo local de la depuración final del repositorio (2026-08):
# versiones intermedias del manuscrito, carpetas de trabajo y rondas de plan
# superadas. Vive en disco por seguridad, nunca se sube al repositorio.
_respaldo_pre_depuracion_no_versionado/